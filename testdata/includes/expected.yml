commands:
    hello:
        config:
            retries: 3
            timeout: 30
        description: A simple command that imports from a file when packed.
        steps:
            - run:
                command: |+
                    #!/bin/bash
                    echo "Hello World"

                name: Hello Greeting
            - run:
                command: |+
                    #!/bin/bash
                    echo "Validating..."

                name: Validate
common:
    defaults:
        retries: 3
        timeout: 30
