entities:
  item1:
    entity:
      attributes:
        description: A simple entity that imports from a file when packed.
      config:
        retries: 3
        timeout: 30
      id: example1
      steps:
        - run:
            command: |
              #!/bin/bash
              echo "Hello World"
            name: Hello Greeting
        - run:
            command: |
              #!/bin/bash
              echo "Validating..."
            name: Validate
shared:
  defaults:
    retries: 3
    timeout: 30
