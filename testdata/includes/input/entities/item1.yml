entity:
  id: example1
  attributes:
    description: A simple entity that imports from a file when packed.
  config: !include ../shared/defaults.yml
  steps:
    - run:
        name: Hello Greeting
        command: !include-text ../scripts/hello.sh
    - run:
        name: Validate
        command: <<include(../scripts/validate.sh)>>
